package day05;

public class functionPush {

    public static void main(String[] args) {

        //origianl에 기존 배열의 주소를 넣어준다.
        //type으로 기존 배열과 신규 배열의 타입을 입력받아야 함.
        //newData로 입력할 추가데이터를 변수 n으로 받아야 함.

/*
        type[] original;
        type newData = n;

        //1. 원본 배열보다 사이즈가 1개 큰 새로운 배열을 생성
        type[] temp = new type[original.length + 1];

        //2. 기존 배열 데이터를 복사해서 신규 배열에 저장.
        for (int i = 0; i < original.length; i++) {
            temp[i] = arr[i];
        }
        //3. 추가할 데이터를 마지막 위치에 저장.
        temp[temp.length - 1] = newData;

        //4. 기존 배열 변수가 신규 배열의 주소를 가질 수 있도록 설정.
        original = temp;
        //5. 신규 배열 변수가 더이상 신규 배열을 제어할 수 없도록 설정.
        temp = null;

 */

    }
}
